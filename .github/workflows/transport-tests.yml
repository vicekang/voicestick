name: Transport Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  protocol:
    name: Protocol and state-machine tests
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v5
      - name: Run transport tests
        run: ./scripts/test-transport.sh
