# VoiceStick Linux

Linux implementation workspace.

Initial direction:

- C++ app implementation.
- BLE through BlueZ / D-Bus.
- Text insertion needs separate handling for X11 and Wayland.
- Packaging choice is still open.

Shared code should be extracted only after Windows and Linux have enough concrete overlap to justify it.
